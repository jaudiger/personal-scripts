#!/usr/bin/env nu
#
# Copyright (c) Jérémy Audiger.
# All rights reserved.
#

use mod.nu *

const ZIG_GLOBAL_CACHE_FOLDER = $"($nu.home-dir)/.cache/zig"
const ZIG_LOCAL_CACHE_FOLDER = ".zig-cache"
const ZIG_LOCAL_BUILD_FOLDER = "zig-out"

# Cleanup all the build directories of the current directory recursively
def "main cache cleanup" [
    --path: string=$GIT_FOLDER # The path to clean
    --dry-run # Do not cleanup the build directories
    --quiet # Do not print the build directories cleaned
]: nothing -> nothing {
    # Global cleanup
    if ($ZIG_GLOBAL_CACHE_FOLDER | path exists) {
        if not $quiet {
            print $"Cleaning global directory ($ZIG_GLOBAL_CACHE_FOLDER)"
        }

        if not $dry_run {
            rm -r $ZIG_GLOBAL_CACHE_FOLDER
        }
    }

    # Local cleanup
    let local_directories = glob $"($path)/**/{($ZIG_LOCAL_CACHE_FOLDER),($ZIG_LOCAL_BUILD_FOLDER)}"

    $local_directories | each {|local_directory|
        if not $quiet {
            print $"Cleaning cache directory ($local_directory)"
        }

        if not $dry_run {
            rm -r $local_directory
        }
    } | ignore
}

def main []: nothing -> nothing {}
